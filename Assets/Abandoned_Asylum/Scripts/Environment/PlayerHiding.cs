using UnityEngine;

public class PlayerHiding : MonoBehaviour
{
    public bool isHidden = false;
    // Toggle this manually in Inspector during testing
    // We'll add proper input after fixing the input system
}